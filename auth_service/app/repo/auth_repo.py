# class AuthRepo:
#     def __init__(self):
#         pass

#     async def encode_jwt(
#             self,
#             payload: dict,
#             private_key: str = all_settings.private_key_path.read_text(),
#             algorithm: str = all_settings.algorithm,
#             expire_minutes: int = all_settings.access_token_expire_minutes,
#             expire_timedelta: timedelta | None = None,
#             ) ->:
